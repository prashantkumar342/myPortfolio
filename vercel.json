{
  "build": {
    "env": {
      "DATABASE_URL": "mongodb+srv://tuhaniaji:tuhaniaji@portfolio.1i5is.mongodb.net/portfolioDB?retryWrites=true&w=majority&appName=portfolio"
    }
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".next"
      }
    }
  ],
  "commands": {
    "build": "npm run build && npx prisma generate"
  }
}